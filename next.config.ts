import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ["bifacial-larisa-warriorlike.ngrok-free.dev"],
};

export default nextConfig;
